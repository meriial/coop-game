{
  "name": "workshop-game",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "login": "DRY_RUN=1 bash setup.sh",
    "login:dev": "WORKER_URL=http://localhost:8787 DRY_RUN=1 bash setup.sh",
    "dev:server": "pnpm --filter server dev",
    "dev:starter": "pnpm --filter starter dev",
    "dev:frontend": "pnpm --filter frontend dev",
    "deploy": "pnpm --filter frontend build && pnpm --filter server deploy",
    "test": "npm run test --prefix server && npm run test --prefix packages/mcp-server"
  }
}
